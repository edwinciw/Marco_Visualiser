{
  "name": "macro-visualiser",
  "description": "Vue 3 SPA for macro charts. scripts: dev=Vite HMR; build=vue-tsc then vite build; preview=static preview. dependencies=Vue+Router+Pinia+ECharts. devDependencies=Vite, @vitejs/plugin-vue, TypeScript, vue-tsc.",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "echarts": "^5.5.1",
    "pinia": "^2.2.6",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.3",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
